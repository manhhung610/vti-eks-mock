apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: mock-app
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: mock-eks
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 80
      targetPort: http

