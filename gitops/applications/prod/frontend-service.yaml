apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: mock-app
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: mock-eks
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http

