# Backend

Small Node.js API used by the mock EKS GitOps platform.

## Endpoints

- `GET /` returns a plain health message.
- `GET /api/health` returns health information.
- `GET /api/status` returns environment and database configuration status.
- `GET /api/items` returns mock project tasks.

